import Image from "next/image";

import Profile from "@/components/ui/profile.client"
export default function ProfilePage() {
    return (
      <main>
        <Profile /> {/* Correct component usage */}
      </main>
    );
  }